apiVersion: v1
kind: Service
metadata:
  name: tetris
  namespace: default
  labels:
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app: tetris
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80